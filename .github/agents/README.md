# Copilot custom agents (workspace)

> **REPEALED 2026-08-13.** GameMan, IsoVisualLoop, and RefactorMan are
> disabled (`user-invocable: false`, `disable-model-invocation: true`).
> Current law: root [`AGENTS.md`](../../AGENTS.md). Living design:
> [`docs/intent/`](../../Docs/intent/README.md).

Do not re-enable these personas. Do not add a new default implementer
that patches `src/` surgically. Recovery first, then rewrite on
`rewrite/intent-first`.
